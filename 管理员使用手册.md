# 云南春招智能学习平台 · 管理员使用手册

> 面向系统运营与管理员，涵盖**部署上线、支付接入、运营看板**三大板块。
> 当前域名处于备案阶段，本文档按「备案通过后上线」的完整流程编写，备案期间可先在本地/测试服务器完成全部联调。

---

## 一、系统概览

平台已形成完整商业闭环：

```
用户注册（邀请码） → 获得积分 → 打卡/成就/邀请持续赚积分
        ↓
积分兑换 AI 次数包（消费出口）          VIP 会员开通（支付）
        ↓                                      ↓
   AI 答疑 / AI 练习 / 学习计划 / 学情分析      无限 AI + 专属权益
        ↓                                      ↓
        运营数据看板（用户/活跃/营收/AI 用量/积分）← 管理员
```

| 模块 | 说明 |
| --- | --- |
| VIP 会员 | 月卡/季卡/年卡，开通后 AI 能力无限使用 |
| 积分体系 | 注册 20、打卡 10、成就 10~150、邀请 50/人；可兑换 AI 次数包 |
| 邀请机制 | 邀请人 +50 积分，被邀请人 +20 积分 |
| 支付渠道 | 支持 演示模式 / 微信支付 v3 / 支付宝，通过环境变量一键切换 |
| 运营看板 | `/admin` 页面，需管理员手机号鉴权 |

---

## 二、部署上线流程（域名备案通过后）

### 1. 服务器准备

- 一台公网服务器（Linux/Windows 均可），Node.js ≥ 22（需内置 `node:sqlite`）
- 一个已备案域名，解析到服务器 IP

### 2. 配置 HTTPS（支付回调强制要求）

微信支付与支付宝的**回调地址必须为 HTTPS**。推荐使用 Nginx 反向代理：

```nginx
server {
    listen 443 ssl;
    server_name www.yourdomain.com;

    ssl_certificate     /etc/nginx/cert/fullchain.pem;
    ssl_certificate_key /etc/nginx/cert/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

### 3. 配置站点地址

编辑 `E:\saixt\server\.env`：

```
BASE_URL=https://www.yourdomain.com
```

> `BASE_URL` 用于生成支付回调地址与分享链接，**必须与备案域名一致**。

### 4. 启动服务

```bash
cd E:\saixt\server
npm install
node src/index.js
```

启动日志会显示支付渠道状态，例如：

```
[saixt-server] 支付渠道: wechat（参数已配置）
```

---

## 三、环境变量配置总览

编辑 `E:\saixt\server\.env`（参考 `.env.example`）：

| 变量 | 必填 | 说明 |
| --- | --- | --- |
| `DEEPSEEK_API_KEY` | ✅ | DeepSeek 密钥，AI 答疑/练习/计划/分析 |
| `BASE_URL` | ✅ | 站点对外地址（备案域名） |
| `ADMIN_PHONES` | ✅ | 管理员手机号（逗号分隔），访问运营看板 |
| `PAY_PROVIDER` | ✅ | `demo` / `wechat` / `alipay` |
| `WECHAT_APPID` | 微信时 | 微信开放平台/公众号 AppID |
| `WECHAT_MCH_ID` | 微信时 | 微信支付商户号 |
| `WECHAT_SERIAL_NO` | 微信时 | API 证书序列号 |
| `WECHAT_API_V3_KEY` | 微信时 | APIv3 密钥（32 位） |
| `WECHAT_PRIVATE_KEY` / `WECHAT_PRIVATE_KEY_PATH` | 微信时 | 商户私钥（二选一） |
| `ALIPAY_APP_ID` | 支付宝时 | 支付宝应用 AppID |
| `ALIPAY_PRIVATE_KEY` | 支付宝时 | 应用私钥（RSA2，PKCS8） |
| `ALIPAY_PUBLIC_KEY` | 支付宝时 | 支付宝公钥 |
| `ALIPAY_GATEWAY` | 否 | 网关（沙箱联调可换沙箱地址） |

---

## 四、微信支付接入（PAY_PROVIDER=wechat）

### 1. 申请商户号

在 [微信支付商户平台](https://pay.weixin.qq.com) 注册商户号，开通 **Native 支付** 产品。

### 2. 获取四类参数

| 参数 | 获取位置 |
| --- | --- |
| AppID | 微信开放平台/公众号后台，需与商户号完成绑定 |
| 商户号 mchid | 商户平台首页 |
| API 证书序列号 | 商户平台 → 账户中心 → API安全 → API证书 |
| APIv3 密钥 | 商户平台 → API安全 → APIv3密钥（自行设置 32 位） |

### 3. 下载商户私钥

在 API 安全 → 申请 API 证书，下载 `apiclient_key.pem`（商户私钥）。

### 4. 写入 .env

```
PAY_PROVIDER=wechat
WECHAT_APPID=wx1234567890abcdef
WECHAT_MCH_ID=1900000001
WECHAT_SERIAL_NO=1234567890ABCDEF
WECHAT_API_V3_KEY=your32charapiv3key
# 推荐用文件路径方式（密钥不落库）
WECHAT_PRIVATE_KEY_PATH=E:/certs/apiclient_key.pem
```

### 5. 配置回调地址

在商户平台 → 产品中心 → Native 支付 → 开发配置中，将回调地址设为：

```
https://www.yourdomain.com/api/membership/pay/notify/wechat
```

### 6. 验证

- 前端 VIP 页点击「立即开通」→ 弹出微信扫码二维码
- 微信扫码支付 → 回调自动置订单为已支付 → 会员立即生效
- 运营看板「支付渠道」显示「微信支付 · 参数已配置」

---

## 五、支付宝接入（PAY_PROVIDER=alipay）

### 1. 创建应用

在 [支付宝开放平台](https://open.alipay.com) 创建「网页/移动应用」，开通 **电脑网站支付** 产品。

### 2. 配置密钥

应用详情 → 开发设置 → 接口加签方式：

- 生成应用私钥（RSA2），将**公钥**上传给支付宝
- 保存支付宝返回的**支付宝公钥**

### 3. 写入 .env

```
PAY_PROVIDER=alipay
ALIPAY_APP_ID=2021000123456789
ALIPAY_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
ALIPAY_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----"
```

### 4. 配置回调地址

在支付宝应用 → 开发设置 → 接口加签方式 → 授权回调地址中配置：

```
https://www.yourdomain.com/api/membership/pay/notify/alipay
```

### 5. 沙箱联调（可选）

使用沙箱环境：`ALIPAY_GATEWAY=https://openapi-sandbox.dl.alipaydev.com/gateway.do`，配合沙箱账号测试完整支付流程后再切换正式网关。

---

## 六、运营看板使用说明

访问 `https://www.yourdomain.com/#/admin`（需登录管理员手机号）。

### 核心指标

| 指标 | 说明 |
| --- | --- |
| 注册用户 | 总量 / 今日新增 / 近 7 天新增 |
| 今日活跃 | 今日刷题人数 / 次数 |
| VIP 会员 | 有效会员数 / 转化率 |
| 累计营收 | 已支付订单金额 / 笔数 |
| 今日 AI 调用 | 今日 / 近 7 天 AI 用量 |
| 积分发放 | 累计发放 / 已消耗 |
| 支付渠道 | 当前渠道与参数配置状态（绿色=可收款，黄色=未配置） |

### 趋势图

近 14 天「新增注册 / 活跃用户 / AI 调用」切换查看，用于评估运营活动效果。

### 订单列表

最近 50 笔订单，含用户、金额、状态（已支付/待支付/已取消）、支付方式、支付时间。

### 用户列表

最近 100 个用户，含刷题量、积分、打卡数、VIP 状态。

---

## 七、常见问题排查

| 现象 | 排查 |
| --- | --- |
| 支付二维码不显示 | 检查 `PAY_PROVIDER` 与微信四类参数是否完整；查看服务日志 `[pay] 创建支付失败` |
| 支付回调不生效 | 确认 `BASE_URL` 为 HTTPS 备案域名；确认回调地址已在商户平台配置；查看日志 `[pay] 回调异常` |
| 微信回调验签失败 | 确认 `WECHAT_API_V3_KEY` 与商户平台设置一致 |
| 支付宝验签失败 | 确认 `ALIPAY_PUBLIC_KEY` 为「支付宝公钥」而非应用公钥 |
| 运营看板 403 | 确认当前登录手机号在 `ADMIN_PHONES` 中 |
| 会员开通后 AI 仍受限 | 确认订单状态为「已支付」；刷新页面查看 VIP 标识 |

---

## 八、版本记录

- **V2.1（当前）**：接入真实支付渠道（微信支付 v3 / 支付宝），支付参数配置化，前端支持扫码/跳转/轮询；运营看板新增支付渠道状态
- **V2.0**：商业闭环完善（VIP 会员、积分体系、邀请机制、AI 次数包、运营看板）
